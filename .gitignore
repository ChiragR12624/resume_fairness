# Python cache & venv
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.env

# Jupyter checkpoints
.ipynb_checkpoints/

# System files
.DS_Store

# Data — ignore big/raw files, but keep sample CSV
data/*
!data/sample_cleaned_resume.csv

# Models — ignore big binaries, but keep folder + small demos
models/*
!models/.gitkeep

# Reports — keep final report, metrics, screenshots
reports/*
!reports/final_report.pdf
!reports/metrics_summary.csv
!reports/dashboard_screenshots/*
!reports/.gitkeep

# Notebooks — keep all
# (no ignore, because we want them tracked)

# Viz — keep Streamlit/Plotly code
# (no ignore)

# Logs
*.log

# MacOS system files
.DS_Store

# Large numpy arrays
*.npy

# Temporary / scratch notebooks
Untitled*.ipynb

